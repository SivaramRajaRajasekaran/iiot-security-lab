# Tells Mosquitto which port to listen on
# and that we want password authentication
listener 1883

# This is the KEY security change
# Before: anyone could connect without credentials
# After: connection refused without username/password
allow_anonymous false

# Points to the file containing valid usernames and passwords
# Mosquitto reads this file on startup
password_file /mosquitto/config/passwd